# Finance Tracker

A mobile-friendly monthly expense tracker that runs as a static web app.

## Features

- Add expenses with date, amount, category, and description
- Filter by month
- Track monthly budget and daily spending pace
- View category breakdowns
- Export the selected month to CSV
- Store data privately in the browser with local storage

## Run locally

Open `index.html` in a browser. No build step is required.
